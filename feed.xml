---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ site.url }}/</id>
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author>
    <name>{{ site.social.name | xml_escape }}</name>
    <uri>{{ site.url }}/</uri>
  </author>
  <link rel="self" type="application/atom+xml" href="{{ site.url }}/feed.xml"/>
  <link rel="alternate" type="text/html" hreflang="{{ site.lang }}" href="{{ site.url }}/"/>
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <rights> © {{ site.time | date: '%Y' }} {{ site.social.name }} </rights>
  <icon>/assets/img/favicons/favicon.ico</icon>
  <logo>/assets/img/favicons/favicon-96x96.png</logo>

{% assign posts = site.posts | where: "lang", site.default_lang | sort: 'date' | reverse %}
{% for post in posts limit: 20 %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
  {% if post.last_modified_at %}
    <updated>{{ post.last_modified_at | date_to_xmlschema }}</updated>
  {% else %}
    <updated>{{ post.date | date_to_xmlschema }}</updated>
  {% endif %}
    <id>{{ site.url }}{{ post.url }}</id>
    <content type="html" xml:base="{{ site.url }}{{ post.url }}">{{ post.content | xml_escape }}</content>
    <author>
      <name>{{ post.author | default: site.social.name | xml_escape }}</name>
    </author>

  {% if post.category %}
    <category term="{{ post.category | xml_escape }}" />
  {% endif %}
  {% if post.summary %}
    <summary>{{ post.summary | strip_html | normalize_whitespace | xml_escape }}</summary>
  {% elsif post.excerpt %}
    <summary>{{ post.excerpt | strip_html | normalize_whitespace | xml_escape }}</summary>
  {% endif %}
  </entry>

{% endfor %}
</feed>
